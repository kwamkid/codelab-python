# ═══════════════════════════════════════════════════════════
# 📐 main.py — เครื่องคิดเลขพื้นที่ห้อง
# F2 Session 08 · Project (Multi-file)
# ═══════════════════════════════════════════════════════════
#
# 🎯 ไฟล์นี้คือ "โปรแกรมหลัก" — เรียก function จาก my_tools.py
# ✨ ของใหม่: บรรทัดแรก import function จากอีกไฟล์!
# ───────────────────────────────────────────────────────────

# TODO 1: import function ที่ต้องใช้จาก my_tools
from my_tools import area_rectangle, area_circle

print("=== เครื่องคิดเลขพื้นที่ห้อง ===")
print("ลองวัดห้องนอนจริงของหนูดูนะ! (หน่วยเมตร)")

# TODO 2: รับความกว้าง และ ความยาว ของห้อง
width = float(input("ความกว้างห้อง (เมตร): "))
length = float(input("ความยาวห้อง (เมตร): "))

# TODO 3: เรียก area_rectangle เพื่อหาพื้นที่ห้อง
room = area_rectangle(width, length)
print("พื้นที่ห้องของหนู =", room, "ตารางเมตร")

# ✨ Bonus: ถ้าจะปูพรมกลม รัศมี 1 เมตร ใช้พื้นที่เท่าไหร่?
print("พรมกลมรัศมี 1 เมตร =", area_circle(1), "ตารางเมตร")
