# ═══════════════════════════════════════════════════════════
# 🏠 HW1 (บังคับ) · Money Converter — บาท ↔ ดอลลาร์
# F2 Session 08 · Functions  ·  เวลา ~20 นาที
# ═══════════════════════════════════════════════════════════
#
# 🎯 โจทย์: เขียน 2 function แปลงเงิน
# 📌 ก่อนทำ: ถามพ่อแม่ว่า "วันนี้ 1 ดอลลาร์ = กี่บาท?"
#            แล้วเขียนคำตอบไว้ใน comment ข้างล่าง
#
#  อัตราแลกเปลี่ยนวันนี้ (ถามพ่อแม่): 1 USD = ______ บาท
#
# ❌ ห้ามใช้ AI / ChatGPT ในการบ้านบังคับ
# ───────────────────────────────────────────────────────────

def baht_to_usd(baht, rate):
    # TODO 1: คืนค่าเงินดอลลาร์  (baht หาร rate)
    usd = ____
    return str(baht) + " บาท = " + str(usd) + " USD"

def usd_to_baht(usd, rate):
    # TODO 2: คืนค่าเงินบาท  (usd คูณ rate)
    baht = ____
    return str(usd) + " USD = " + str(baht) + " บาท"


# ───────────────────────────────────────────────────────────
# ทดสอบ (ใช้ rate = 35 เป็นตัวอย่าง — เปลี่ยนเป็นของจริงได้)
print(baht_to_usd(350, 35))   # ควรได้ "350 บาท = 10.0 USD"
print(usd_to_baht(20, 35))    # ควรได้ "20 USD = 700 บาท"

# ✅ เกณฑ์เสร็จ:
#  - รันได้ไม่มี error
#  - มี 2 function + ใช้ return
#  - เขียน rate ที่ถามพ่อแม่ไว้ใน comment ด้านบน
