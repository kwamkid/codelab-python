# ═══════════════════════════════════════════════════════════
# 🖥️ W1 · นับสระในชื่อ — text analysis พื้นฐาน
# F2 Session 09 · String Methods
# ═══════════════════════════════════════════════════════════
#
# 🎯 หน้าที่ของเด็ก: เติมโค้ดตรง  # TODO
# string ก็วนได้เหมือน list! ลองนับสระในชื่อตัวเองดู
# ───────────────────────────────────────────────────────────

name = input("พิมพ์ชื่อของหนู: ")

vowels = "aeiou"
count = 0

# TODO 1: วน string ทีละตัวอักษร  (ใช้ .lower() กันตัวพิมพ์ใหญ่)
for char in name.lower():
    # TODO 2: ถ้า char เป็นสระ ให้ count เพิ่ม 1
    if char in vowels:
        count = ____   # 👈 แก้ตรงนี้: count + 1

print("ชื่อ", name, "มีสระทั้งหมด", count, "ตัว")

# ✅ ทดสอบ: "Aim" -> มีสระ 1 ตัว / "Code" -> 2 ตัว
