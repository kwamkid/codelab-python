# ═══════════════════════════════════════════════════════════
# 🏠 HW1 (บังคับ) · Shopping Cart V3 — รวม if + math
# F1 Session 05 · Workshop Day  ·  ~20 นาที
# ═══════════════════════════════════════════════════════════
# 🎯 จำนวน × ราคา → ส่วนลดตามยอด (if) → + VAT 7%
# ❌ ห้ามใช้ AI ในการบ้านบังคับ
# ───────────────────────────────────────────────────────────

qty = int(input("จำนวนชิ้น: "))
price = float(input("ราคาต่อชิ้น: "))

total = qty * price

# ส่วนลดตามยอด
if total >= 1000:
    total = total * 0.9        # ลด 10%
elif total >= 500:
    # TODO: ลด 5%
    total = total * ____       # 👈 แก้ตรงนี้: 0.95

# บวก VAT 7%
total = total * 1.07

print(f"ยอดสุทธิ (รวม VAT): {round(total, 2)} บาท")
