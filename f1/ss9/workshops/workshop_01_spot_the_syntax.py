# ═══════════════════════════════════════════════════════════
# 🐛 W1 · Spot the Syntax — อ่าน error + แก้
# F1 Session 09 · Review & Bug Hunt
# ═══════════════════════════════════════════════════════════
# 🎯 แต่ละข้อมี bug — รันแล้วอ่าน error → แก้ตรง ____
#    (Programmer ตัวจริงใช้เวลา 50% ไป debug!)
# ───────────────────────────────────────────────────────────

# 🐛 BUG 1 — NameError: พิมพ์ชื่อตัวแปรผิด
name = "Aim"
print(____)          # 👈 แก้: name  (อย่าพิมพ์ naem)

# 🐛 BUG 2 — ลืม : ท้าย if (SyntaxError)
score = 8
if score > 5:        # 👈 อย่าลืม : ท้ายบรรทัด
    print("เก่งมาก!")

# 🐛 BUG 3 — ลืมย่อหน้า (IndentationError)
for i in range(3):
    print(i)         # 👈 บรรทัดใน for ต้องย่อหน้า

# ✅ แก้ครบ 3 จุด → รันได้ไม่มี error
